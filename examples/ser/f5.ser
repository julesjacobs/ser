request foo
{
    if (?)
        {
            // X := (X + 1) % 3
            if(X == 0){ X := 1 } else { if(X == 1){ X := 2 } else { X := 0 }};
            X
        }
    else
        {
            // X := (X + 2) % 3
            if(X == 0){ X := 2 } else { if(X == 1){ X := 0 } else { X := 1 }};
            X
        }
}
